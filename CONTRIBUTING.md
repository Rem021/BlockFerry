# 参与 BlockFerry

感谢你帮助改进 BlockFerry。

## 提交问题

请提供版本、启动器、Minecraft 版本、模组及其版本、可复现步骤和期望结果。发布日志或截图前，务必移除用户名、绝对路径、服务器地址、令牌、聊天和世界数据。

## 提交代码

1. 从 `main` 创建短分支。
2. 一次提交只解决一个明确问题。
3. 为解析、计划、事务或 UI 合约变化添加随机临时夹具测试。
4. 运行 `pwsh ./scripts/Test-All.ps1`。
5. 在拉取请求中说明安全边界、兼容范围和人工验证方式。

开发测试不得读取或写入真实 Minecraft/PCL 实例。需要文件系统场景时，只使用测试进程创建的随机临时目录，并证明它没有落入正式实例根。

## 适配器原则

- 只迁移可明确解释为玩家个人状态的数据。
- 先验证目标模组和格式，再生成计划。
- 未知格式、scope 歧义或双方冲突必须阻止或要求显式决定。
- 不复制旧模组 JAR、脚本、账号信息、世界或启动器私有数据库。
- 预览必须幂等且零写入；真实变更只能通过事务协调器。

提交贡献即表示你同意按仓库许可证发布该贡献。
