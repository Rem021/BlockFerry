# 更新记录

本项目遵循语义化版本，Beta 期间兼容范围仍可能调整。

## 0.1.0-beta.5 - 2026-08-30

- 深色与浅色主页改用用户选定的 3172×1984 无损 PNG 原图，并锁定原始字节校验；应用不再将背景转成 JPEG 或做无意义放大，保留玻璃、石材和光线纹理的真实细节。
- 修复部分 Windows、显卡和远程桌面环境切换深色/浅色主题时，重复重建 Mica 与同步刷新动态主题资源导致的崩溃。
- 主题切换使用双缓冲：旧背景与旧遮罩会在主题资源变化前完整覆盖画面，新图按代次解码成功后才平滑淡出；缩放解码与主题切换之间会复用稳定旧帧并在结束后补做尺寸检查，同时移除整个内容层的明暗脉冲，消除黑白切换空帧和二次闪烁。
- 鼠标毛玻璃光晕由固定 16 ms 界面线程计时器改为随显示器刷新同步的合成层位移；进一步拆成遮罩下方的小型柔和光核与低亮度雾光拖尾，使用保留速度、刷新率无关的解析弹簧，让快速移动产生约 100–140 ms 的惯性而不形成刺眼的大白斑。
- 深色与浅色主页底图保持大留白、右侧积木构图；运行时让 WinUI 按真实客户区自动选择解码尺寸，并对拖动缩放做 140 ms 防抖，避免手工过度解码后的二次缩小模糊。

## 0.1.0-beta.4 - 2026-08-27

- 首个公开测试版。
- PCL2 有限自动探测和手动目录选择。
- Vanilla、JEI 19.x、Dark Mode Everywhere 1.x 与 Extreme Sound Muffler 3.x 个人设置迁移。
- 无需先启动目标实例的 JEI scope 识别与安全复核路径。
- 只读预览、最终清单、认证备份、原子提交、复读验证、恢复和撤销。
- 启动时只读检查官方 GitHub Releases；仅提示，不自动下载或执行。
- 清理公开交付结构；界面统一使用 SIL OFL 1.1 的 Noto Sans SC。
- 增加应用内中英文即时切换，并覆盖发现、选择、恢复、JEI 延后复核和安全事务状态。
- 搜索、设置读取和只读预览增加加载动画；真实同步显示事务完成步数对应的确定进度。
- 修复快速切换语言时的中英混排，并重新对齐标题栏语言按钮与展开图标。
- 大型同步改用轻量进度刷新和按需展开明细；加宽主要设置面板，避免数百项清单导致界面假死或拥挤。
- 将右侧抽屉重构为全宽三阶段迁移工作区，审核、执行进度与结果不再挤在设置列表中；统一自定义标题栏控件与 Windows 原生按钮基线。
- 重新验证实例与清单时，首页使用整条底部进度通道，不再在装饰版本数字上显示重复的“正在处理”按钮。
- 同步设置增加一个全局“全选”入口；只批量选择无冲突项目，冲突仍要求逐项确认。
- 已验证完成页增加“再次同步”，保留当前来源与目标并重新只读检查内容，不复用上一次清单。
