# BlockFerry 隐私说明

生效版本：`0.1.0-beta.5`

BlockFerry 不提供账号系统，不包含广告或遥测 SDK，也不把 Minecraft 配置、实例路径、JEI 收藏、设备标识或使用统计上传给项目维护者。

## 本机读取

只有在自动探测或用户选择文件夹后，应用才会读取受能力边界限制的 PCL2/Minecraft 元数据与受支持设置文件。发现和预览阶段不会修改实例。应用不会读取浏览器数据、启动器账号令牌、Cookie、密码、世界存档或整块磁盘。

## 本机写入

执行同步前会显示最终清单。用户明确确认后，应用只写清单内经过再次验证的目标文件，并在 `%LOCALAPPDATA%\BlockFerry` 保存认证备份、事务日志、恢复/撤销依据、主题和已保护的最近位置。

删除 `%LOCALAPPDATA%\BlockFerry` 会同时删除恢复与撤销依据；存在未完成事务时请勿先删除。

## 网络访问

BlockFerry 只在以下两种场景主动联网：

1. 每次程序启动最多一次，向 `https://api.github.com/repos/Rem021/BlockFerry/releases` 读取公开 Release 元数据，用于显示新版本入口。请求不带 GitHub 登录、访问令牌、Minecraft 数据或设备标识；失败时不重试，也不影响同步。
2. 当 JEI 收藏来自局域网多人 scope，且来源日志提供最后连接的 IP 地址时，应用可能向该 IP 和端口发送一次标准 Minecraft 状态查询，以识别同一服务器在目标实例中的 scope 名称。请求不包含账号、会话令牌或登录数据；识别失败时对应内容保持阻止或待复核。

应用不会自动下载或执行更新。点击新版本入口后，系统默认浏览器会打开经过严格限定的 `github.com/Rem021/BlockFerry/releases/...` 页面，后续访问受浏览器和 GitHub 隐私政策约束。

## 日志和问题报告

应用不会自动上传日志。若你主动在 GitHub 提交问题，请先删除用户名、绝对路径、服务器 IP/域名、令牌、聊天内容和世界数据。

本说明如有重大变化，会随 Release 一并记录。
