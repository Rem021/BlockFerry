# Beta 测试指南

BlockFerry 的自动测试只使用随机临时夹具，不读取或写入真实 Minecraft/PCL 实例。真实兼容性验证应使用实例副本，并保留原始目录。

## 推荐测试矩阵

每组来源与目标至少覆盖以下两种状态：

- 目标实例刚安装、从未启动。
- 目标实例已成功进入过一次主菜单或服务器。

优先验证：

1. 发现与预览不会改变来源或目标中的任何文件。
2. Vanilla 界面尺寸、语言、按键、音量和辅助功能在重启游戏后仍有效。
3. JEI 单人收藏与可识别的多人 scope 在重启游戏、重新进入世界或服务器后仍存在。
4. Dark Mode Everywhere 与 Extreme Sound Muffler 的受支持设置在重启后仍有效。
5. 同一计划重复执行不会产生额外变化。
6. 模拟失败后，恢复或撤销能还原目标文件。

## 提交兼容性结果

请使用仓库的“兼容性结果”表单，并提供：

- BlockFerry、Minecraft、加载器、整合包和相关模组版本；
- 目标实例是否启动过；
- 选择的同步类别；
- 游戏重启后的实际结果。

不要上传完整实例、账号数据、世界、聊天日志、服务器地址、访问令牌或包含用户名的绝对路径。若必须提供配置样本，请先制作最小、虚构且可公开的复现文件。
